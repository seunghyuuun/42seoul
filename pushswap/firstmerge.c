#include "push_swap.h"

void firstmerge(t_stack *abstack)
{
    t_updown udinfo;

    while (abstack->atop)
    {
        udcheck(abstack, &udinfo);
        if (udinfo.a_top[0] == udinfo.a_bottom[0])
            
    }
}